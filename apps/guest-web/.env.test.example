# E2E 테스트 환경변수 — apps/guest-web/.env.test 로 복사 후 값 채우기.
# .env.test 는 .gitignore 에 의해 커밋 안 됨(있어야 함 — 확인 필요).
#
# 적용처: playwright.config.ts(dotenv) + e2e/auth.setup.ts(시드 유저 로그인)
# 컨벤션: _code_convention/TESTING.md § 시드 유저 / 운영 - 8. 환경 가정

# dev Supabase (memory: cvtcogtdbaimcckjkzqy)
VITE_SUPABASE_URL=https://cvtcogtdbaimcckjkzqy.supabase.co
VITE_SUPABASE_ANON_KEY=

# 시드 유저 비밀번호 (supabase/seed.sql 의 password 와 동기화 필수)
E2E_TEST_GUEST_EMAIL=test-guest@example.com
E2E_TEST_GUEST_PASSWORD=test-pass-123

E2E_TEST_HOST_EMAIL=test-host@example.com
E2E_TEST_HOST_PASSWORD=test-pass-123

E2E_TEST_COHOST_EMAIL=test-cohost@example.com
E2E_TEST_COHOST_PASSWORD=test-pass-123

# (선택) baseURL 오버라이드 — 기본 http://localhost:5201
# E2E_BASE_URL=http://localhost:5201
