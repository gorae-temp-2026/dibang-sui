# Photo Sharing — UI 이식 (2026-05-22)

## 무엇을 했나
와이어프레임이던 `SharePhotoUploadPage.tsx`의 시각 구조를 `web-mobile-application/apps/web-app/src/pages/photos/PhotoSharePage.tsx`(레거시)의 모양으로 이식. 데이터 로직은 무수정.

## 변경 파일
- `apps/dibang-wedding/src/pages/SharePhotoUploadPage.tsx` (리라이트)
- `apps/dibang-wedding/src/components/share-photos/PhotoProgressOverlay.tsx` (신규)
- `apps/dibang-wedding/src/lib/sharedPhotoUrl.ts` (신규)

## 보존된 것 (데이터 로직 무수정 게이트)
- `machines/sharePhotoUpload.machine.ts`, `lib/presignedUpload.ts`, `packages/contracts/**`, `components/share-photos/SharePhotosTab.tsx`, `pages/LoungeV2Page.tsx`, `App.tsx` 라우트, `apps/api/server/handler_shared_photos.go` 모두 diff 0줄.

## 의도적으로 뺀 것 (현재 SDK에 없는 정보)
- 게스트 이름·소속 배지 (`guestInfo`)
- 캡션 입력 (`createSharedPhoto` body에 caption 없음)
- 신랑/신부 이름 (완료 화면 문구) — 일반 문구로 대체

## 새로 더한 것 (현재 와이어프레임 강점 보존)
- 사진별 진행률 오버레이 (`PhotoProgressOverlay`): queued/converting/requesting/uploading%/done/failed 상태별 표시
- 업로드 중 화면: 상단 전체 진행률 바 + 하단 사진별 진행률 그리드 동시 표시

## 토큰 매핑
원본 `colors.*` → 현재 `lng-*` Tailwind 토큰 (App.css `@theme` 정의). brand 컬러 동일(`#D4687A`).

## 검증
- `pnpm --filter dibang-wedding build` 통과
- `pnpm --filter dibang-wedding lint` 통과
- Playwright 스크린샷: idle/selecting/uploading/done 4종 (`.playwright-mcp/share-photos-*.png`). error 상태는 presignedUpload 자체 catch로 일반 경로 도달 불가 — 코드 경로만 존재.
